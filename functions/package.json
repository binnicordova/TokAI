{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "npm run build && firebase emulators:start --only functions",
    "server": "npm run build && ts-node src/index.ts",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "devDependencies": {
    "@types/express": "^5.0.3",
    "node-fetch": "^3.3.0",
    "socket.io-client": "^4.7.2",
    "typescript": "^5.7.3"
  },
  "private": true,
  "dependencies": {
    "express": "^5.1.0",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.4.0",
    "socket.io": "^4.8.1",
    "tiktok-live-connector": "^2.0.0"
  }
}
